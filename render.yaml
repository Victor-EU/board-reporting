services:
  - type: web
    name: board-reporting
    runtime: node
    plan: free
    buildCommand: npm ci && npx prisma generate && npm run build
    startCommand: npm run db:migrate && npm run db:seed && npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: postgresql://telbase:cfUL4IQm417e8fcWm8u5WlnSWYAwVa9j@dpg-d5t2givfte5s73co7ak0-a/telbase
